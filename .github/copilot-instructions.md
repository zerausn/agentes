Use `AGENTS.md` as the repo entrypoint.
If `AI.md` exists, read it too.
Use `docs/DECISIONS.md`, `docs/PROGRESS.md`, and `docs/HANDOVER.md` when relevant.
If you work inside a subproject with its own `AGENTS.md`, the nearest file wins.
Do not commit secrets or local-only memory files.
